interface:
  display_name: "Clean Your Data"
  short_description: "Explain local paths before changing them"
  default_prompt: "Use $clean-your-data to explain why this path exists, what evidence supports that conclusion, and what could break if it moves."
policy:
  allow_implicit_invocation: false
