interface:
  display_name: "Clean Your Data: Full Audit"
  short_description: "Run the legacy machine-wide storage audit"
  default_prompt: "Use $audit-local-files to create a redacted, read-only machine-wide storage report."
policy:
  allow_implicit_invocation: false
