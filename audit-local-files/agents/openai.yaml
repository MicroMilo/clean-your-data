interface:
  display_name: "Clean Your Data"
  short_description: "Map local data and plan safe cleanup"
  default_prompt: "Use $audit-local-files to map my local data, explain the risks, and propose safe next actions without deleting anything."
policy:
  allow_implicit_invocation: true
