interface:
  display_name: "Clean Your Data"
  short_description: "Map local data and review reversible cleanup"
  default_prompt: "Use $audit-local-files to map my local data, separate scan evidence from agent advice, and propose exact reversible cleanup candidates. Never move anything without my confirmation."
policy:
  allow_implicit_invocation: true
