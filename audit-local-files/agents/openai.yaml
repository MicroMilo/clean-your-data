interface:
  display_name: "Audit Local Files"
  short_description: "Analyze local file and app storage safely"
  default_prompt: "Use $audit-local-files to analyze my local file organization without deleting anything."
policy:
  allow_implicit_invocation: true
